{
  "name": "@eth-optimism/specs-2",
  "version": "0.4.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/prettier": "^2.2.3",
    "prettier": "^2.3.1",
    "lint-staged": "11.0.0"
  },
  "scripts": {
    "lint": "yarn lint:fix && yarn lint:check",
    "lint:fix": "yarn prettier --write '**/*.md'",
    "lint:check": "yarn prettier --check '**/*.md'"
  }
}

